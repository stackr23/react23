// import gulp         from 'gulp'
// import gulpGhPages  from 'gulp-gh-pages'

// const {paths: {build: buildPath}} = require('config').default

// TBD: run sh script "deploy_to_github"
// (cd dist; git add .)
// (cd dist; git commit -m "Build output as of $(git log '--format=format:%H' master -1)"
// git push origin gh-pages
//

// const deployToGithub = done =>
//     gulp.src(buildPath + '/**/*')
//         .pipe(gulpGhPages({
//             force: true
//         }))

// gulp.task('gh-pages', gulp.series(deployToGithub))

// export default deployToGithub
