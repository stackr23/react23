jobs:
  prebuild:
    docker:
      - image: circleci/node:12.9.0
    steps:
      - run: npm install gulp-cli && npm install
  build:
    docker:
      - image: circleci/node:12.9.0
    steps:
      - checkout
      - run: npm run build
    requires:
      - prebuild
  test_full:
    docker:
      - image: circleci/node:12.9.0
    steps:
      - checkout
      - run: npm install gulp-cli && npm install && npm run test-full

workflows:
  version: 2
  setup:
  build_and_test:
    jobs:
      - test_full
